<h1><%= @task.title %></h1>
<%# we want to show the details and completed status %>
<p>
  <%= @task.details %>
</p>
<p>
  <%= @task.completed ? "Completed" : "Incomplete" %>
</p>
<%# we want to show the edit and delete links %>
<p>
  <%= link_to "Edit", edit_task_path(@task), class: "btn btn-primary" %>
</p>
<%= link_to "Back to Tasks", tasks_path, class: "btn btn-primary mt-2" %>
